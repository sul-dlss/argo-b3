<div class="container">

  <%= render SdrViewComponents::Elements::HeadingComponent.new(level: :h1, variant: :h4, text: 'Groups') %>

  <ul class="list-unstyled">
    <% current_user.groups.each do |group| %>
      <li><%= group %></li>
    <% end %>
  </ul>
</div>
