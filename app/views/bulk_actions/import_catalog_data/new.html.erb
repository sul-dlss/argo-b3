<%= form_with model: @bulk_action_form, url: bulk_actions_import_catalog_data_path do |form| %>
  <%= render BulkActions::FormComponent.new(bulk_action_config: @bulk_action_config, form:, with_close_version: true) do |component| %>
    <%= render SdrViewComponents::Forms::FileComponent.new(form:,
                                                           field_name: :csv_file,
                                                           label_text: 'Upload a CSV or Excel file',
                                                           help_text: 'Each row in the spreadsheet should include a druid column, and optionally folio_instance_hrid, barcode, refresh, part_label, and sort_key columns.') %>
  <% end %>
<% end %>
