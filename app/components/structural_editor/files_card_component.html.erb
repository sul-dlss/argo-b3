<div class="card" id="files-card">
  <div class="card-body">
    <% if files.present? %>
      <ul class="list-unstyled">
        <% files.each do |file| %>
          <li><%= file %></li>
        <% end %>
      </ul>
    <% else %>
      <p>No files.</p>
    <% end %>
  </div>
</div>
