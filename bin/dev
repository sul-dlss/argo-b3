#!/usr/bin/env sh

# USER_GROUPS should be semicolon-separated (no spaces).
USER_GROUPS="${USER_GROUPS:=sdr:argo-access;sdr:argo_administrators}" NAME="${NAME:=A. User}" EMAIL="${EMAIL:=a.user@stanford.edu}" bin/overmind start