#!/usr/bin/env sh

# USER_GROUPS should be semicolon-separated (no spaces).
USER_GROUPS="${USER_GROUPS:=sdr:argo-access;sdr:administrator-role}" NAME="${NAME:=A. User}" EMAIL="${EMAIL:=a.user@stanford.edu}" bin/overmind start