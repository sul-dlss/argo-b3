<%= tag.th scope: 'col', class: classes, style: do %>
  <%= label %>

  <%# Tooltip component isn't used currently, so did not import the component. %>
  <%# = render Elements::TooltipComponent.new(target_label: label, tooltip:) %>
<% end %>
